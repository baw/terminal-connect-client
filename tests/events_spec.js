require("./mock_server.js");
require("./mock_child_process.js");

describe("connects to server", function () {
    
});

describe("emits initial command event to server", function () {
    
});

describe("emits 'terminal-error' event to server for command error", function () {
    
});

describe("emits 'terminal-output' event to server for command output", function () {
    
});

describe("responds to 'commandID' event from server", function () {
    
});

describe("responds to 'error' event from server", function () {
    
});